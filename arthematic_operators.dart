void main() {
  // Now I will declare and intialize the variables

  int a, b, c, x, y;

  a = 10;
  b = 5;

  // Now I will add two numbers

  c = a + b;
  print(c);

  // Now I will subtract two numbers

  x = a - b;
  print(x);

  // Now I will multiply two numbers

  y = a * b;
  print(y);

  // Now I will divide two numbers

  double z = a / b;
  print(z);
}
